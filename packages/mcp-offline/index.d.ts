/**
 * @coderover/mcp-offline
 *
 * A dependency-only companion to `@coderover/mcp`. Installing this
 * package is equivalent to installing `@coderover/mcp` + pulling in
 * the `@xenova/transformers` runtime needed for `--embed offline`
 * mode. No runtime exports beyond a version string.
 */
export declare const version: string;
